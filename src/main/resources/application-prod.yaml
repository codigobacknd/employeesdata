spring:
  datasource:
    url: jdbc:mysql://db:3306/employees
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect

logging:
  level:
    root: INFO
    hibernate:
      SQL: debug
      type: trace